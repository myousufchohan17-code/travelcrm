PORT=4000
JWT_SECRET=change-this-to-a-long-random-secret
JWT_EXPIRES_IN=7d
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=travel_crm
CLIENT_ORIGIN=http://localhost:5173
UPLOAD_DIR=uploads
# Production on Vercel uses Neon Postgres via DATABASE_URL
# The AI Travel Assistant is fully API-free: it runs locally with no API keys.
